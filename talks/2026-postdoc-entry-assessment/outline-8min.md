---
title: "2026 博士后进站考核 · 8 分钟汇报逐帧内容草稿"
tags:
  - talk
  - slides-outline
  - postdoc-entry-assessment
status: draft
date: 2026-06-24
---

# 8 分钟汇报内容草稿（两部分结构）

> 本文件只定**内容**，不写 LaTeX。定稿后再据此改 `template.tex`。
> 现有 `template.tex` 共 28 张内容帧（约 30 分钟量级），本草稿压到 **10 张内容帧 + 封面/简介/结束页**，目标 **8 分钟**。

## 元信息（封面）

- **主标题**：结构拓扑优化的高精度数值方法与高性能求解（伞形，覆盖 Part 1 高精度离散 + Part 2 PIML/Matrix-Free 高性能；原"基于高阶有限元的变密度拓扑优化"仅覆盖 Part 1，已弃用）
- **副标题**：改为 **「博士后进站考核 · 科研工作汇报与研究计划」**（原为"博士期间科研工作汇报"）
- **报告人 / 导师 / 单位**：何亮；博士导师 魏华祎 教授、合作导师 郭旭 院士；单位双校署名——湘潭大学 数学与计算科学学院（博士）· 大连理工大学 力学与航空航天学院（博士后）
- **时长**：约 8 分钟

## 视觉基调（已锁定）

**全场无框**（box-free）：不用 tcolorbox 填充卡片/色条，层级靠"字号 + 粗细 + 留白 + 细线 + 左色条"。已封装 4 个辅助命令复用：`\blocktitle`（粗体彩色标题 + 细线）、`\accentintro`（左色条引导）、`\takeaway`（居中粗体结论）、`\imglabel`（图注）。Part 2 沿用同一套。
> 决策依据：无框更可读、更现代；正式感来自内容严谨与结构清晰，而非画框。已对比满框版后确认（2026-06-24）。

## 结构改造（相对现版）

1. 两个 `\section`：**「一、博士期间研究工作」**（Part 1）、**「二、博士后科研计划」**（Part 2）。
2. **删除** `\AtBeginSection` 逐节自动目录页 + 全局目录页（省 ~5 张过场）。
3. 封面、个人简介、结束页保留。
4. 时间分配：封面 ~0:20 → 个人简介 ~0:40 → Part 1 ~3:15 → Part 2 ~2:30 → 结束 ~0:20（合计 ~7:05，留余量）。
5. **个人简介**作为正式开场帧（帧 1，置于 Part 1 章节页之前）。
6. **不设独立的背景/路线页**：原「研究背景·关键问题·研究路线」与帧 1 的研究主线 2×2 重复，已删；背景与四大关键问题改由帧 1→方法一的口头过渡承担。

## 帧清单与时间预算

| # | 帧标题 | 时长 | 由现有哪些帧而来 |
|---|--------|------|------------------|
| — | 封面 | 0:20 | 保留 |
| 1 | 个人简介（教育/经历 + 研究方向 + 研究主线） | 0:40 | 提升原"个人简介"卡片页（补教育/工作经历） |
| **Part 1 — 博士期间研究工作** | | **~3:40** | |
| 2 | 方法一·任意次拉格朗日高阶有限元 | 0:45 | `任意次拉格朗日FEM`+`阶次对比overlay` |
| 3 | 方法二·高阶多分辨率框架 | 0:45 | `精度错配`+`多分辨率框架`+`表达优势` |
| 4 | 方法三·任意次胡张混合有限元 | 0:50 | `从位移法到混合`+`胡张混合`+`抗闭锁`/`应力评估` |
| 5a | SOPTX 软件平台（架构+关键技术+GPU） | 0:45 | `设计理念`+`总体架构`+`核心模块`+`GPU加速`+`多后端`/`AD` |
| 5b | 博士成果概览（论文/软著/项目） | 0:35 | `成果总览`+`论文`+`软著`+`工程应用` |
| **Part 2 — 博士后科研计划** | | **~2:30** | |
| 6 | 科研计划总览与承接关系 | 0:40 | 新建（源自 `postdoc-research-plan.md`） |
| 7 | 方向一·PIML 增强多尺度分析 + Matrix-Free 高性能求解 | 0:40 | 新建 |
| 8 | 方向二·MMC/MMV 显式拓扑优化先进数值分析 | 0:35 | 新建 |
| 9 | 工作计划·预期目标·平台依托 | 0:35 | 新建 |
| — | 结束页（敬请批评指正） | 0:20 | 保留 |

> 帧 2「研究背景·关键问题·研究路线」已删（与帧 1 的研究主线 2×2 重复）；背景/四问题改为帧 1→方法一的口头过渡。帧 5 拆为 5a（平台）/ 5b（成果）。净 **10 内容帧**，合计 **~7:30**。

> 删除/并入的 18 张帧：不同离散方式比较（密度表征/三角形 overlay）、密度映射鲁棒性、多分辨率应力约束应用、复杂边界低阶适用、局部应力评估（并入帧4）、研发背景设计理念、总体架构、核心模块、高效矩阵组装、自动微分、多后端一致性（并入帧5）、代表性成果总览、论文成果、软件著作权、工程应用、以及旧"未来计划"两帧（论文产出基金、软件平台产学研——内容方向不符，整体替换为 Part 2）。

---

# Part 1 — 博士期间研究工作

## 帧 1 · 个人简介（开场帧）

**核心一句话**：何亮，计算数学 / 拓扑优化方向，围绕"基于高阶有限元的变密度拓扑优化"开展系统研究，形成方法—软件—实践一体的研究积累。

**版式建议**：左照片 + 姓名 + 研究方向；右"博士阶段·研究主线"+"博士后·研究方向"两块。学历/导师/单位**不在本页重复**（已在封面写全：博士导师 魏华祎、合作导师 郭旭院士、双校署名）。

**左栏：个人信息**
- 照片 + 姓名（何亮）；
- **研究方向**：偏微分方程数值方法 / 连续体结构拓扑优化 / 科学计算软件设计与实现。

**右栏：两阶段研究地图（开场即预告全场）**
- **博士阶段·研究主线（2×2）**：① 高阶有限元拓扑优化方法 ② 多分辨率拓扑优化框架 ③ 胡张混合有限元拓扑优化方法 ④ SOPTX 软件平台设计与实现；
- **博士后·研究方向**（精确镜像计划书两个题目）：① PIML 增强多尺度结构分析与 Matrix-Free 高性能求解 ② MMC/MMV 显式拓扑优化的高精度数值离散与高效结构分析。

> 设计说明：原"教育/工作经历"已删（信息移至封面，避免重复）；原"研究目标"替换为"博士后·研究方向"，使简介同时覆盖两部分、与 Part 2 自然衔接。

**讲稿要点**：~40 秒自我介绍——先认人，再用右栏两块"博士做了什么 / 博士后要做什么"给全场定调。**过渡到方法一时口头补一句背景+问题**："拓扑优化广泛用于航空航天、建筑、生物医疗等承力结构轻量化设计；面向精度受限、分辨率与规模难兼顾、应力约束困难、缺乏统一软件四个问题，我做了对应的三方法 + 一平台。"——以此替代被删的独立背景/路线页。

---

## 帧 2 · 方法一 · 任意次拉格朗日高阶有限元

**核心一句话**：将状态方程从低阶位移元推广到任意次拉格朗日元，统一高阶离散与优化建模，提升分析精度。

**配图**：阶次对比三图（`topo_k1` / `topo_k2` / `topo_k4`，MBB 梁、无过滤、统一四边形单元与体积约束）。

**要点 / 数据（图注）**
- 线性元 $k=1$：$n=53,\ c\approx203.07$ —— 棋盘格明显、边界锯齿突出；
- 二阶元 $k=2$：$n=36,\ c\approx213.18$ —— 构型质量明显改善、伪结构减少；
- 四阶元 $k=4$：$n=41,\ c\approx218.66$ —— 相较 $k=2$ 改善有限，拓扑细节未同步提升。

**底部结论条**：提高分析阶次有改善作用，但**收益受单分辨率设计空间限制**——自然引出方法二。

**讲稿要点**：高阶分析能改善力学场，但"分析阶次↑、设计分辨率没跟上"，所以下一帧讲多分辨率解耦。（密度表征/三角形单元两组对比此处**删去**，只保留阶次这条主线。）

---

## 帧 3 · 方法二 · 高阶多分辨率拓扑优化框架

**核心一句话**：引入"位移网格—密度积分网格—设计变量网格"三层离散，解耦分析与设计，兼顾高精度分析与高分辨率拓扑表达。

**配图**：`multires_framework` 三层网格示意（主图）+ 可选 `mbb_mtop_k4`（高分辨率构型）作小图对照。

**三层网格（精简三栏）**
- $\mathcal{T}_h$ 高阶位移网格：较粗高阶单元，控制分析自由度规模；
- $\mathcal{T}_\rho$ 密度积分网格：子单元密度积分，保证刚度组装一致性；
- $\mathcal{T}_d$ 设计变量网格：承载高分辨率设计变量，提升拓扑表达。

**表达优势数据（可放图注一行）**：MBB 梁，STOP($k=1$) $\rho_{\text{dof}}=600$ vs MTOP($k=4$) $\rho_{\text{dof}}=9600,\ u_{\text{dof}}=19762$ —— 同分析规模下设计自由度大幅提升、构型更清晰。

**底部结论条**：三层解耦在控制计算规模的同时，释放高阶分析优势，获得更清晰、细节更丰富的拓扑构型。

**讲稿要点**：核心卖点是"解耦"。原"密度映射鲁棒性""L 型支架应力约束应用"两帧**删去**（应力话题交给方法三）。

---

## 帧 4 · 方法三 · 任意次胡张混合有限元

**核心一句话**：以应力为独立未知量、构造 $H(\mathrm{div})$ 协调对称应力空间的任意次胡张混合元，嵌入变密度拓扑优化闭环，直接获得高精度应力场。

**配图**：抗闭锁对比 `bearing_disp_nu4999` vs `bearing_mixed_nu4999`（二维轴承，近不可压 $\nu_0=0.4999$）；如版面够，附应力场 `stress_vm_disp` vs `stress_vm_mixed` 小图。

**左：方法（精简）**
- 以应力 $\boldsymbol\sigma$ 为独立未知量，构造 $H(\mathrm{div})$ 协调对称张量有限元空间（北大胡俊教授方法，完成程序实现并引入拓扑优化）；
- 基于单纯形晶格统一实现任意阶次；每步迭代同时求解 $\boldsymbol\sigma_h,\boldsymbol u_h$，直接由 $\boldsymbol\sigma_h$ 构造柔顺度目标与局部应力约束。

**右：两项代表性能力（数据）**
- **抗体积闭锁**：近不可压 $\nu_0=0.4999$ 下，标准位移法刚度高估、构型异常（$c\approx55.78$）；胡张混合法构型稳定（$c\approx102.79$）。
- **应力场可靠评估**：位移法应力场单元间非物理跳跃；混合法基于协调应力空间，应力场平滑连续、约束评估更可靠（悬臂梁 V 型承载构型）。

**底部结论条**：实现"胡张混合分析 → 变密度拓扑优化闭环"系统拓展，为高精度应力驱动拓扑优化奠定基础。

**讲稿要点**：抗闭锁 + 应力评估两个亮点各一句。原"复杂边界与低阶稳定化"帧**删去**（技术细节，口头一句带过即可）。

---

## 帧 5a · SOPTX 软件平台

**核心一句话**：以"分析—优化解耦"为核心构建模块化 SOPTX 平台，支持 AD、多后端与 CPU/GPU 异构。

**版式建议**：上"架构 + 关键技术"双栏，下 GPU 加速表 + 结论条。

- **架构三层**：用户交互层 / 核心算法层（插值-分析-正则化-优化闭环）/ FEALPy 基础后端层；
- **关键技术**：高效张量化矩阵组装、PyTorch/JAX 自动微分灵敏度、NumPy/PyTorch/JAX 多后端与 CPU/GPU 异构；
- **性能亮点（GPU 加速表，三维悬臂梁）**：总耗时 2789.9 s → **636.7 s**，端到端 **≈4.38×**；稳态平均迭代 21.09 s → 4.84 s，**≈4.36×**；
- 多后端构型/收敛一致（NumPy/PyTorch/JAX）；AD 与手动微分结果一致、效率相当、扩展更便捷（口头一句）。

**底部结论条**：模块化解耦 + AD + 多后端异构，为高阶有限元拓扑优化提供高效可扩展软件支撑。

**讲稿要点**：讲"解耦 + GPU 4.38× + 多后端一致"三点即可，~40 秒。

---

## 帧 5b · 博士成果概览

**核心一句话**：贯通"数学模型—数值方法—软件实现—科研实践"，形成论文、软著、项目的完整成果链。

**版式建议**：三卡片（论文 / 软著 / 项目）或左右两栏 + 底部链条结论条。数字优先、条目从简。

- **论文**：一作 *SOPTX: A Modular and Extensible Framework for Topology Optimization with Multi-Backend Support*，**CiCP 40(2): 525–572**；参与 *Adaptive FEM for phase field fracture …*（JCAM 2026, 472:116732）、*FEALPy: A Cross-platform Intelligent Numerical Simulation Engine*（CiCP 已录用）。
- **软著**：一作《结构拓扑优化仿真计算软件》（登记号 2025SR0887352，第一完成人）；参与《建筑结构力学仿真计算软件》（2024SR0961048，第三完成人）。
- **工程/项目**：中建五局建筑结构力学 C++ 计算内核（集成中建智拓平台，主要参与人）；FEALPy 计算固体力学库底层架构与多本构求解器（主要参与人）。

**底部结论条**：贯通"数学模型—数值方法—软件实现—科研实践"研究链条，为 Part 2 的方向承接做铺垫。

**讲稿要点**：用数字快速带过（3 篇论文 / 2 项软著 / 2 个项目），不逐条念，~35 秒。

---

# Part 2 — 博士后科研计划

> 来源：`research/postdoc-research-plan.md`。两大研究方面、四条主线、三阶段、三目标，依托郭旭院士团队（MMC/MMV、PIML、SiPESC、DLUTopt）与算海团队（FEALPy）。

## 帧 6 · 科研计划总览与承接关系

**核心一句话**：面向大规模与复杂结构拓扑优化，开展「PIML 增强多尺度结构分析 + Matrix-Free 高性能求解」与「MMC/MMV 显式拓扑优化先进数值分析」两大方面研究，与博士工作直接承接。

**配图（新建·示意图待补）**：建议一张"承上启下"图——左列博士积累，右列博后方向，箭头连接。

**承接关系（建议用箭头表）**
| 博士积累 | → 博后延伸 |
|---|---|
| 任意次高阶 / 胡张混合有限元 | MMC/MMV 复杂几何的先进数值离散（虚单元/混合/高阶元） |
| 高阶多分辨率框架（分析-设计解耦） | MMC/MMV 高效结构分析（多分辨率/自适应/子结构/信息复用） |
| SOPTX 多后端 + GPU 异构 | 全局算子 **Matrix-Free** 实现 + CPU/GPU/异构并行 |
| —（新增能力） | **PIML** 按需预测多尺度形函数、构造粗尺度等效刚度 |

**总体目标（一句）**：形成兼顾计算效率、并行可扩展性与分析精度的算法原型，并完成典型算例验证。

**讲稿要点**：先点"两大方面"，再强调"不是另起炉灶"——博士的高阶/混合/多分辨率/SOPTX 正是博后的基础，PIML 是新增的关键增量。

---

## 帧 7 · 方向一 · PIML 增强多尺度分析 + Matrix-Free 高性能求解

**核心一句话**：面向大规模拓扑优化中反复细网格分析的高成本，用 PIML 在线预测多尺度形函数、构造粗尺度等效刚度，并进一步发展全局算子 Matrix-Free 高性能求解。

**配图（新建·示意图待补）**：PIML 多尺度流程图——"局部细尺度密度 → PIML 预测多尺度形函数 → 粗尺度等效刚度 → 粗网格平衡方程求解 → 细尺度位移/灵敏度恢复"。

**主线 1：PIML 多尺度形函数预测与粗尺度等效刚度**
- 以粗网格单元/子结构内部已知细尺度材料密度为输入，离线训练 PIML 模型在线预测多尺度形函数，减少重复求解局部细尺度问题；
- 重点研究等效刚度的对称性、正定性、能量一致性、误差传播；分析粗细尺度比、局部边界条件、预测误差的影响。

**主线 2：从无持久存储到全局算子 Matrix-Free**
- 复现并优化"按需预测—算后释放 + 全局缩聚刚度组装 + PETSc 迭代求解"基线流程；
- 进一步研究**不显式组装全局缩聚矩阵**的 Matrix-Free 算子作用，结合 Krylov 迭代 + 多层预条件，面向多核 CPU / GPU / 异构的并行高性能求解。

**讲稿要点**：一句点"PIML 替代重复局部求解"，一句点"Matrix-Free 把存储/规模瓶颈再降一层"，与博士 SOPTX 的 GPU 能力衔接。

---

## 帧 8 · 方向二 · MMC/MMV 显式拓扑优化先进数值分析

**核心一句话**：面向 MMC/MMV 显式几何描述与分析模型的耦合，研究高精度数值离散与高效结构分析方法，提升复杂几何、应力约束与几何非线性问题的精度与效率。

**配图（新建·示意图待补）**：MMC/MMV 显式组件 + 边界切割单元离散示意。

**主线 3：MMC/MMV 高精度数值离散**
- 研究几何边界投影、边界切割单元处理、数值积分与灵敏度的一致性，评估几何近似/离散误差影响；
- 将**虚单元、混合有限元、高阶有限元**与 MMC/MMV 显式描述结合，提升复杂几何与应力约束问题的精度与稳定性（直接承接博士方法一、三）。

**主线 4：MMC/MMV 高效结构分析**
- 利用 MMC/MMV 边界清晰、设计变量维数低、几何演化显式可控的特点，研究分析模型高效更新与快速求解；
- 集成多分辨率离散、自适应局部加密、子结构分析与求解信息复用；**将方向一的 Matrix-Free 迁移应用于 MMC/MMV**，实现两方面在求解技术上的相互支撑。

**讲稿要点**：强调"显式几何 + 先进离散"，并点出第四主线借方向二的 Matrix-Free 形成闭环。

---

## 帧 9 · 工作计划 · 预期目标 · 平台依托

**核心一句话**：分三阶段推进，形成两套方法 + 一套可复用算法/软件模块体系，依托郭旭团队与算海团队的互补优势落地。

**三阶段工作计划（每段一句）**
1. **第一阶段**：文献调研、基准复现（全尺度/多尺度/PIML 增强分析、MMC/MMV 代表方法）、建立统一算例与性能评估框架；
2. **第二阶段**：PIML 多尺度分析与 Matrix-Free 求解方法研究 + MMC/MMV 先进数值分析方法研究；
3. **第三阶段**：并行实现与软件集成（CPU/MPI → GPU/异构）、典型二维/三维算例验证与比较。

**预期目标（三条，浓缩）**
- 目标一：建立 PIML 增强多尺度分析 + Matrix-Free 高性能求解方法（含稳定性/误差准则、并行原型）；
- 目标二：建立 MMC/MMV 高精度离散 + 高效结构分析方法（含一致灵敏度、虚单元/混合/高阶离散方案）；
- 目标三：形成算法原型、可复用软件模块与统一典型算例体系，衔接 FEALPy / SiPESC / DLUTopt。

**平台依托（一行）**：郭旭院士团队（力学建模、MMC/MMV、PIML、SiPESC / DLUTopt 工业软件）× 算海团队（计算数学、FEALPy）——力学方法与数值计算互补。

**讲稿要点**：阶段讲节奏、目标讲产出、平台讲底气，收束到"理论—算法—程序—验证"完整链条。

---

## 结束页

保留现版："科研工作汇报结束 / 敬请各位专家老师批评指正！"

---

# 决策记录与待确认

**已定：**
1. ✅ 副标题：「博士后进站考核 · 科研工作汇报与研究计划」。
2. ✅ 帧 5 拆为 **5a（平台+GPU）/ 5b（成果概览）** → 总 10 内容帧、约 7.5 分钟。
3. ✅ Part 2 三张示意图先用**纯文字框 + 箭头版式**，矢量图后补。

**待确认（不阻塞 Part 1 写作）：**
4. **一作论文是否还有未列出的**（现仅 SOPTX 一篇一作 + 两篇合作）？暂按已记录条目写，后续可补。
5. **基金/项目口径**：Part 2 是否一律以 `postdoc-research-plan.md` 为准、删掉旧帧的"湖南省/韶峰天工/青年基金"等湘潭语境内容？（Part 2 阶段再定。）
